#pragma once
#include <vector>

// Return the sum of elements at indices 0,2,4,...
// Empty vector -> 0
int sum_at_even_indices(const std::vector<int>& v);

// Return a new vector where each element is doubled.
std::vector<int> doubled(const std::vector<int>& v);
